using Fletched.Core.IR;

namespace Fletched.Emitters;

/// <summary>Emits expression evaluation code.</summary>
public sealed class ExpressionEmitter : IEmitter
{
    /// <inheritdoc/>
    public string Emit(PlanBlock block) => string.Empty;
}
