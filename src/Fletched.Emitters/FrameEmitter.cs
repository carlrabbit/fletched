using Fletched.Core.IR;

namespace Fletched.Emitters;

/// <summary>Emits resumable frame struct declarations.</summary>
public sealed class FrameEmitter : IEmitter
{
    /// <inheritdoc/>
    public string Emit(PlanBlock block) => string.Empty;
}
