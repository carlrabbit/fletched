using Fletched.Core.IR;

namespace Fletched.Emitters;

/// <summary>Emits typed state struct declarations.</summary>
public sealed class StateEmitter : IEmitter
{
    /// <inheritdoc/>
    public string Emit(PlanBlock block) => string.Empty;
}
