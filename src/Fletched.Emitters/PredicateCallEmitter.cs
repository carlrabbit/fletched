using Fletched.Core.IR;

namespace Fletched.Emitters;

/// <summary>Emits predicate call and resume logic.</summary>
public sealed class PredicateCallEmitter : IEmitter
{
    /// <inheritdoc/>
    public string Emit(PlanBlock block) => string.Empty;
}
